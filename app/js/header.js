(function(){
  let header = document.querySelector('.header');

  function scroll(dest, duration = 200, easing = 'linear', callback) {
    const start = window.pageYOffset;
    const startTime = new Date().getTime();
  }

  header.addEventListener();
  header.addEventListener();
})();
